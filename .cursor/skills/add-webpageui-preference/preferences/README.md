# Preferences

One markdown file per UI preference. Indexed by the User preferences lookup table in [../webpage-debugging/webpageui-standard.md](../webpage-debugging/webpageui-standard.md).

Managed by `add-webpageui-preference`.
